# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# tfvars hold account-specific config; commit only the examples
*.tfvars
!*.tfvars.example

# Ansible
ansible/out/
*.retry
.vault_pass
ansible/vault-password

# Certs and keys never belong in git
*.pem
*.key
*.crt
*.csr

.DS_Store

ssh-keys
