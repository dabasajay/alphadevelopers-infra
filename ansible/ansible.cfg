[defaults]
inventory = inventory/hosts.yml
roles_path = roles
host_key_checking = True
# The community.general yaml callback was removed; this is its replacement.
result_format = yaml
interpreter_python = auto_silent
retry_files_enabled = False

[ssh_connection]
pipelining = True
